import { manusAxios } from '../config/manus';\n\nexport interface ManusAIRequest {\n  prompt: string;\n  model?: string;\n  max_tokens?: number;\n  temperature?: number;\n}\n\nexport interface ManusAIResponse {\n  id: string;\n  result: string;\n  tokens_used: number;\n  created_at: string;\n}\n\nexport interface ContentSuggestion {\n  caption: string;\n  hashtags: string[];\n  best_posting_time: string;\n  content_type: string;\n  engagement_score: number;\n}\n\n/**\n * Manus AI Service - AI-powered content generation and optimization\n */\nexport class ManusAIService {\n  /**\n   * Generate AI-powered caption for TikTok video\n   * @param videoDescription Description or context of the video\n   * @param targetAudience Target audience for the video\n   * @returns Generated caption with hashtags\n   */\n  static async generateCaption(\n    videoDescription: string,\n    targetAudience: string = 'general'\n  ): Promise<ContentSuggestion> {\n    try {\n      const prompt = `Generate a viral TikTok caption for: \"${videoDescription}\". \n      Target audience: ${targetAudience}. \n      Include relevant hashtags and emojis. \n      Make it engaging, concise, and optimized for TikTok's algorithm.\n      Response format: {\"caption\": \"...\", \"hashtags\": [...], \"engagement_score\": 0-100}`;\n\n      const response = await manusAxios.post<ManusAIResponse>('/generate', {\n        prompt,\n        model: 'gpt-4',\n        max_tokens: 200,\n        temperature: 0.8,\n      });\n\n      // Parse the AI response\n      let parsedResult = { caption: '', hashtags: [], engagement_score: 0 };\n      try {\n        parsedResult = JSON.parse(response.data.result);\n      } catch {\n        parsedResult = {\n          caption: response.data.result,\n          hashtags: ['#TikTok', '#Viral', '#Content'],\n          engagement_score: 75,\n        };\n      }\n\n      const now = new Date();\n      const optimalHour = [9, 12, 15, 18, 21][\n        Math.floor(Math.random() * 5)\n      ];\n      const nextPostTime = new Date(now);\n      nextPostTime.setHours(optimalHour, Math.floor(Math.random() * 60), 0);\n\n      return {\n        caption: parsedResult.caption,\n        hashtags: parsedResult.hashtags,\n        best_posting_time: nextPostTime.toISOString(),\n        content_type: 'video',\n        engagement_score: parsedResult.engagement_score,\n      };\n    } catch (error: any) {\n      console.error('Error generating caption with Manus AI:', error.message);\n      throw {\n        success: false,\n        error: error.response?.data || error.message,\n        message: 'Failed to generate caption with Manus AI',\n      };\n    }\n  }\n\n  /**\n   * Analyze video content for optimal performance\n   * @param videoCaption Caption of the video\n   * @param category Video category/niche\n   * @returns Analysis and recommendations\n   */\n  static async analyzeContent(\n    videoCaption: string,\n    category: string = 'general'\n  ): Promise<any> {\n    try {\n      const prompt = `Analyze this TikTok video caption for viral potential and optimization:\n      Caption: \"${videoCaption}\"\n      Category: ${category}\n      \n      Provide analysis on:\n      1. Hashtag effectiveness\n      2. Emoji usage\n      3. Caption length\n      4. Call-to-action potential\n      5. Estimated engagement rate (0-100)\n      6. Recommendations for improvement\n      \n      Response format: JSON with scores and recommendations`;\n\n      const response = await manusAxios.post<ManusAIResponse>('/analyze', {\n        prompt,\n        model: 'gpt-4',\n        max_tokens: 300,\n        temperature: 0.5,\n      });\n\n      let analysis = JSON.parse(response.data.result);\n      analysis.tokens_used = response.data.tokens_used;\n\n      return {\n        success: true,\n        data: analysis,\n        message: 'Content analysis completed successfully',\n      };\n    } catch (error: any) {\n      console.error('Error analyzing content with Manus AI:', error.message);\n      throw {\n        success: false,\n        error: error.response?.data || error.message,\n        message: 'Failed to analyze content',\n      };\n    }\n  }\n\n  /**\n   * Get trending topics and content ideas from Manus AI\n   * @param niche Content niche/category\n   * @returns List of trending topics and content ideas\n   */\n  static async getTrendingIdeas(niche: string = 'general'): Promise<any> {\n    try {\n      const prompt = `What are the top 10 trending topics and content ideas for TikTok in the \"${niche}\" niche right now?\n      Include:\n      1. Topic/trend name\n      2. Why it's trending\n      3. Best format (dance, lip-sync, educational, comedy, etc.)\n      4. Estimated reach potential\n      5. Hashtags to use\n      \n      Response format: JSON array of trend objects`;\n\n      const response = await manusAxios.post<ManusAIResponse>('/trends', {\n        prompt,\n        model: 'gpt-4',\n        max_tokens: 500,\n        temperature: 0.7,\n      });\n\n      const trends = JSON.parse(response.data.result);\n\n      return {\n        success: true,\n        data: trends,\n        tokens_used: response.data.tokens_used,\n        message: 'Trending ideas retrieved successfully',\n      };\n    } catch (error: any) {\n      console.error('Error fetching trending ideas:', error.message);\n      throw {\n        success: false,\n        error: error.response?.data || error.message,\n        message: 'Failed to fetch trending ideas',\n      };\n    }\n  }\n\n  /**\n   * Generate hashtag recommendations\n   * @param topic Main topic of the video\n   * @param count Number of hashtags to generate\n   * @returns Recommended hashtags with reach estimates\n   */\n  static async generateHashtags(topic: string, count: number = 10): Promise<any> {\n    try {\n      const prompt = `Generate ${count} highly effective TikTok hashtags for content about: \"${topic}\".\n      \n      For each hashtag, provide:\n      1. Hashtag\n      2. Estimated reach (in millions)\n      3. Competition level (low/medium/high)\n      4. Relevance score (0-100)\n      \n      Prioritize less competitive, high-reach hashtags.\n      Response format: JSON array with hashtag objects`;\n\n      const response = await manusAxios.post<ManusAIResponse>('/hashtags', {\n        prompt,\n        model: 'gpt-4',\n        max_tokens: 250,\n        temperature: 0.6,\n      });\n\n      const hashtags = JSON.parse(response.data.result);\n\n      return {\n        success: true,\n        data: hashtags,\n        count: hashtags.length,\n        message: 'Hashtags generated successfully',\n      };\n    } catch (error: any) {\n      console.error('Error generating hashtags:', error.message);\n      throw {\n        success: false,\n        error: error.response?.data || error.message,\n        message: 'Failed to generate hashtags',\n      };\n    }\n  }\n\n  /**\n   * Get optimal posting time based on audience analysis\n   * @param audience Target audience (e.g., 'teens', 'young-adults', 'professionals')\n   * @returns Optimal posting times\n   */\n  static async getOptimalPostingTime(audience: string = 'general'): Promise<any> {\n    try {\n      const prompt = `Based on TikTok analytics for ${audience} audience, what are the optimal times to post?\n      Consider:\n      1. Time zones (UTC)\n      2. Days of week\n      3. Peak engagement hours\n      4. Algorithm boost timing\n      \n      Response format: JSON with recommended posting times and reasoning`;\n\n      const response = await manusAxios.post<ManusAIResponse>('/optimal-time', {\n        prompt,\n        model: 'gpt-4',\n        max_tokens: 200,\n        temperature: 0.5,\n      });\n\n      const timings = JSON.parse(response.data.result);\n\n      return {\n        success: true,\n        data: timings,\n        message: 'Optimal posting time analysis completed',\n      };\n    } catch (error: any) {\n      console.error('Error getting optimal posting time:', error.message);\n      throw {\n        success: false,\n        error: error.response?.data || error.message,\n        message: 'Failed to get optimal posting time',\n      };\n    }\n  }\n}\n